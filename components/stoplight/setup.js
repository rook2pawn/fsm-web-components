//const items = require("./items");

const store = (state, emitter) => {
  /*
  emitter.on("store:buy", (item) => {
    state.status.money.cents = state.status.money.cents - items[item].cost;
    switch (item) {
      case "401k":
        break;
      default:
        break;
    }
  });
  */
};

exports = module.exports = store;
