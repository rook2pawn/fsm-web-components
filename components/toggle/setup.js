const setup = (state, emitter) => {
  /*
  state.toggle = false;
  emitter.on("toggle", () => {
    state.toggle = !state.toggle;
    emitter.emit("render");
  });
  */
};

exports = module.exports = setup;
